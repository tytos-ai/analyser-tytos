# Workspace-only project - no root binary

[workspace]
resolver = "2"
members = [
    "pnl_core",
    "dex_client",
    "persistence_layer",
    "api_server",
    "config_manager",
    "job_orchestrator",
    "zerion_client",
]

